export class Dha {
    public static async getPropsHistoryData(propIds: string, timeStart: string, timeEnd: string): Promise<any> {
        return {}
    }

    public static async getPropsHistoryValueByTime(propIds: string, time: string, mode: number): Promise<any> {
        return {}
    }

    public static async getPropsHistoryValueByTimeList(propIds: string, mode: number, timeList: string): Promise<any> {
        return {}
    }

    public static async getPropsHistoryValue(propIds: string, timeStart: string, timeEnd: string, pcs: number
        , interval: number, iUnit: string, mode: number, minMax: string
        , extraVals: string, sort: string): Promise<any> {

    }
}